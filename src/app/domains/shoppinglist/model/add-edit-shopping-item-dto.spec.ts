import { AddEditShoppingItemDto } from './add-edit-shopping-item-dto';

describe('AddEditShoppingItemDto', () => {
  it('should create an instance', () => {
    expect(new AddEditShoppingItemDto()).toBeTruthy();
  });
});
